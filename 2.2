radius, length = input('원통의 반지름과 길이를 입력하세요 :').split(',')
radius = float(radius)
length = int(length)
area = radius * radius * 3.14
volume = area * length
print('넓이는 {} 입니다.'.format(area))
print('부피는 {} 입니다.'.format(volume))
